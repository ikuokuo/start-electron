import {} from "./types/dom";
import {} from "./types/vue";

export default class Api {
  one: number;
  two: number;

  constructor(one: number, two: number) {
    this.one = one;
    this.two = two;
  }
}
